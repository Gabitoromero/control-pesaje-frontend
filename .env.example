VITE_API_URL=/api
VITE_SOCKET_URL=/
# VITE_API_PROXY_TARGET_1=http://estudio:3000
# VITE_API_PROXY_TARGET_2=http://100.83.143.60:3000

# Interval (in milliseconds) for the tablet session heartbeat sent to the
# backend to keep an active line session alive. Default: 120000 (2 min).
VITE_HEARTBEAT_INTERVAL_MS=120000